CHATGPT ACTIONS SETUP - QUICK REFERENCE
========================================

1. SERVER URL (for OpenAPI schema):
   https://notionmcp.nowhere-else.co.uk/openapi.json

2. AUTHENTICATION:
   Type: Bearer Token
   Token: [Your ACTION_API_TOKEN from .env file]

3. CONNECT NOTION WORKSPACE:
   Visit: https://notionmcp.nowhere-else.co.uk/oauth/start
   This will authorize and return a connection_id

4. REQUIRED PARAMETERS FOR EACH REQUEST:
   - connection_id: Must be in request body (POST/PATCH) or query param (GET)
   - All endpoints return standard envelope: {ok, result, error, meta}

5. EXAMPLE REQUEST STRUCTURE:
   POST /v1/upsert
   {
     "connection_id": "your-connection-id-from-oauth",
     "database_id": "notion-database-id",
     "unique": {"property": "Name", "value": "Item Name"},
     "properties": {
       "Status": {"type": "select", "value": "In Progress"},
       "Due": {"type": "date", "value": "2026-01-10"}
     }
   }

6. AVAILABLE ACTIONS:
   - Search, Create/Get/Update Databases
   - Create/Get/Update Pages, Archive Pages
   - Get/Update/Append Blocks
   - Upsert (create or update)
   - Link pages via relations
   - Bulk operations
   - Jobs (long-running operations)

7. TROUBLESHOOTING:
   - 401/403: Check ACTION_API_TOKEN
   - Connection errors: Verify OAuth completed, check connection_id
   - Server errors: Check docker compose logs api

